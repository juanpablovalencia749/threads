import React from 'react'
import ReactDOM from 'react-dom/client'
import ThreadsApp from './ThreadsApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ThreadsApp />
  </React.StrictMode>,
)
